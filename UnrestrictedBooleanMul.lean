import UnrestrictedBooleanMul.N3TruthTable
import UnrestrictedBooleanMul.N4.Main

/-!
# Published n=4 verification entry point

The default library exports the exact unrestricted values 0, 1, 3, 6, 9
for input lengths 0 through 4. It intentionally does not import n=5 or n=6
research. Build those through their separate, explicitly scoped workflows.
-/
